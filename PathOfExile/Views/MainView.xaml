<UserControl x:Class="PathOfExile.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="700" Height="700" Width="700">
    <UserControl.Resources>
        <Style TargetType="Label">
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            <GroupBox Header="Item Setup">
                <Grid Grid.Row="1" VerticalAlignment="Bottom">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <ComboBox Margin="5" HorizontalAlignment="Left" Grid.Column="2" Width="100" ItemsSource="{Binding Items, UpdateSourceTrigger=PropertyChanged}"/>
                    <Label Grid.Row="2" >Item name:</Label>
                    <TextBox HorizontalAlignment="Left" Width="150" Grid.Column="1" Grid.Row="2" Text="{Binding ItemName,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"></TextBox>
                    <Label Grid.Row="3">Message (single):</Label>
                    <TextBox Grid.Row="3" HorizontalAlignment="Left" Width="500" Grid.Column="1" MinLines="2" AcceptsReturn="True" Text="{Binding MessageSingle,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"></TextBox>
                    <Label Grid.Row="4">Message (multiple):</Label>
                    <TextBox Grid.Row="4" HorizontalAlignment="Left" Width="500" Grid.Column="1" MinLines="2" AcceptsReturn="True" Text="{Binding MessageMulti,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"></TextBox>
                    <Label Grid.Row="5">Link:</Label>
                    <TextBox Grid.Row="5" HorizontalAlignment="Left" Width="500" Grid.Column="1" Text="{Binding Link,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"></TextBox>
                </Grid>
            </GroupBox>

            <GroupBox Grid.Row="1" Header="Scraper">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Row="0">Get interval (minutes):</Label>
                    <TextBox HorizontalAlignment="Left" Width="150" Grid.Column="1" Grid.Row="0" Text="{Binding GetInterval,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"></TextBox>
                    <Label Grid.Row="1">Time since last get:</Label>
                    <TextBox HorizontalAlignment="Left" Width="150" Grid.Column="1" Grid.Row="1" IsEnabled="False" Text="{Binding TimeSinceLastGet,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"></TextBox>
                    <DataGrid Grid.Row="2" Grid.ColumnSpan="2" MinHeight="100" ItemsSource="{Binding ScrapedList,UpdateSourceTrigger=PropertyChanged}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Width="Auto" Header="Name" Binding="{Binding Name}"/>
                            <DataGridTextColumn Width="Auto" Header="Attempted" Binding="{Binding Attempted}"/>
                            <DataGridTemplateColumn Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Button HorizontalAlignment="Right" Content="Remove" Command="{Binding RemoveFromGetCommand}"/>
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Bottom" Grid.Row="3" Grid.Column="0" Content="Remove Attempted" Command="{Binding RemoveAttemptedCommand}"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"  Grid.Row="3" Grid.Column="1">
                        <Button HorizontalAlignment="Right" VerticalAlignment="Bottom"  Content="Scrape!" Command="{Binding ScrapeCommand}"/>
                        <Button HorizontalAlignment="Right" VerticalAlignment="Bottom"  Content="Use!" Command="{Binding UseCurrentScrapedCommand}"/>
                    </StackPanel>
                </Grid>
            </GroupBox>
            <GroupBox Grid.Row="2" Header="Actions">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Row="0">Copy Delay (seconds):</Label>
                    <TextBox HorizontalAlignment="Left" Width="150" Grid.Column="1" Grid.Row="0" Text="{Binding CopyDelay,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"></TextBox>
                    <Label Grid.Row="1">Number of sets:</Label>
                    <TextBox HorizontalAlignment="Left" Width="150" Grid.Column="1" Grid.Row="1" Text="{Binding NumberOfSets,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"></TextBox>
                    <Label Grid.Row="2">Quantity per set:</Label>
                    <TextBox HorizontalAlignment="Left" Width="150" Grid.Column="1" Grid.Row="2" Text="{Binding QuantityPerSet,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"></TextBox>

                    <TextBlock FontSize="20" Grid.Row="4" Grid.Column="1">Set # <TextBlock FontSize="20" FontWeight="ExtraBold" Text="{Binding SetNumber,UpdateSourceTrigger=PropertyChanged}"/></TextBlock>
                    <StackPanel Orientation="Horizontal" Grid.Row="4" Grid.Column="1" HorizontalAlignment="Right">
                        <Button Content="Stop" Command="{Binding StopCopyCommand}" IsEnabled="{Binding IsCopyStarted}"/>
                        <Button Content="Start" Command="{Binding StartCopyCommand}" IsEnabled="{Binding IsCopyStopped}"/>
                    </StackPanel>
                    <DataGrid Grid.Row="3" Grid.ColumnSpan="2" MinHeight="100" ItemsSource="{Binding PlayerContactList,UpdateSourceTrigger=PropertyChanged}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Width="Auto" Header="Player Name" Binding="{Binding PlayerName}"/>
                            <DataGridTextColumn Width="Auto" Header="Item Name" Binding="{Binding ItemName}"/>
                            <DataGridTextColumn Width="Auto" Header="Attempted" Binding="{Binding Attempted}"/>
                            <DataGridTextColumn Width="Auto" Header="Message" Binding="{Binding Message}"/>
                            <DataGridTemplateColumn Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button HorizontalAlignment="Right" Content="Complete" Command="{Binding RemoveFromGetCommand}"/>
                                            <Button HorizontalAlignment="Right" Content="Counter offer" Command="{Binding RemoveFromGetCommand}"/>

                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                </Grid>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>
